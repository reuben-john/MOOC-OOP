using System;

namespace week_2
{
    class Program
    {
        static void Main(string[] args)
        {

            // variables change same way
            // int a = 12;
            // a += 2;
            // a++;


            // while loops the same
            // Console.Write("Number: ");
            // int a = int.Parse(Console.ReadLine());
            // Console.Write("Number: ");
            // int b = int.Parse(Console.ReadLine());
            // while (a <= b)
            // {
            //     Console.WriteLine(a);
            //     a++;
            // }

            //     Console.Write("Pick a number to sum up to: ");
            //     int sum = sumSet(int.Parse(Console.ReadLine()));
            //     Console.WriteLine($"Total: {sum}");

            // methods are mostly the same
            // can still call other methods and such

        }

        // static int sumSet(int max, int min = 1)
        // {
        //     int total = 0;
        //     while (min <= max)
        //     {
        //         total += min;
        //         min++;
        //     }

        //     return total;
        // }
    }
}
